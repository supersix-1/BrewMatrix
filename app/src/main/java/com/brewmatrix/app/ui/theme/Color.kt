package com.brewmatrix.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light Theme — "Morning Brew" ──

val LightBackground = Color(0xFFFAF7F2)       // Warm off-white
val LightSurface = Color(0xFFFFFFFF)           // Cards with warm shadow
val LightPrimary = Color(0xFF3C2415)           // Deep espresso brown
val LightOnPrimary = Color(0xFFFFFFFF)
val LightSecondary = Color(0xFF7A9E7E)         // Muted sage green
val LightOnSecondary = Color(0xFFFFFFFF)
val LightTertiary = Color(0xFFB8860B)          // Copper gold accent
val LightOnTertiary = Color(0xFFFFFFFF)
val LightOnBackground = Color(0xFF1A1A1A)      // Near black text
val LightOnSurface = Color(0xFF1A1A1A)
val LightSecondaryText = Color(0xFF6B6560)     // Warm gray
val LightSubtleBorder = Color(0xFFE8E2DA)      // Warm light gray
val LightError = Color(0xFFC0392B)             // Warm red
val LightOnError = Color(0xFFFFFFFF)
val LightGradientStart = Color(0xFFC17D3A)     // Warm amber
val LightGradientEnd = Color(0xFF6B3A1F)       // Deep roast

// ── Dark Theme — "Late Night Pour" ──

val DarkBackground = Color(0xFF121010)         // Deep charcoal
val DarkSurface = Color(0xFF1E1B18)            // Dark warm gray
val DarkPrimary = Color(0xFFD4A05A)            // Warm gold
val DarkOnPrimary = Color(0xFF1A1A1A)
val DarkSecondary = Color(0xFF8FAF8A)          // Soft olive
val DarkOnSecondary = Color(0xFF1A1A1A)
val DarkTertiary = Color(0xFFD4944A)           // Bright copper
val DarkOnTertiary = Color(0xFF1A1A1A)
val DarkOnBackground = Color(0xFFF0EBE3)       // Warm white
val DarkOnSurface = Color(0xFFF0EBE3)
val DarkSecondaryText = Color(0xFF9C9489)      // Muted tan
val DarkSubtleBorder = Color(0xFF2E2926)       // Deep warm gray
val DarkError = Color(0xFFE74C3C)              // Warm salmon
val DarkOnError = Color(0xFF1A1A1A)
val DarkGradientStart = Color(0xFFE8B86D)      // Bright amber
val DarkGradientEnd = Color(0xFFA0693D)        // Burnished copper
